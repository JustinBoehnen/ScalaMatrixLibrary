import MatrixSource._
import scala.math;

object Main {
  def main(args: Array[String]): Unit = {
    //val m1: Matrix = new Matrix(2, 1)
    //val m2: Matrix = new Matrix(2, 1)
    //m1.data_=(Array(Array(1), Array(1)))
    //m2.data_=(Array(Array(2), Array(2)))

    //Matrix.dotProduct(m1, m2).printMat()
  }
}